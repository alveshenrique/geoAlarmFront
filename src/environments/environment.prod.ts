// HERE I'M GETTING MY API KEY FROM A FILE. REMOVE THIS LINE AND USE YOUR KEY INSTEAD OF API_KEY FROM env.keys FOR USAGE
import {API_KEY} from '../environments/env.keys';

export const environment = {
  production: true,
  API_KEY: API_KEY
};
